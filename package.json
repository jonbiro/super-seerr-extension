{
  "name": "seerr-browser-extension",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --watch --test tests/**/*.test.js"
  },
  "devDependencies": {
    "fast-check": "^3.15.0"
  }
}
